<div id="sidebar">
<h2>Последние статьи <%= put_sidebar_category %></h2>

  <ul>
    <% @sidebar_offers.each do |article| %>
      <li> <%= link_to "#{article.title}", article_path(article)  %> </li>
      <hr />
     <% end %>
   </ul>

<h2>Самое интересное на сайте</h2>
  <ul>
    <% @sidebar_offers.each do |article| %>
      <li> <%= link_to "#{article.title}", article_path(article)  %> </li>
      <hr />
     <% end %>
   </ul>
</div>

